version: "2"

services:
  websq:
    image: akawato2018/web-sq:0.0
    volumes:
      - datastore:/usr/src/app/db
volumes:
  datastore:
    driver_opts:
      type: none
      device: /var/lib/customer/ftp-data/
      o: bind
